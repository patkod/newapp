<%= form_with(model: job) do |form| %>
  <% if job.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :vacancy, style: "display: block" %>
    <%= form.number_field :vacancy %>
  </div>

  <div>
    <%= form.label :sallary, style: "display: block" %>
    <%= form.number_field :sallary %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
